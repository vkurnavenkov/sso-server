%h1
  Hello,
  = signed_in? ? current_user.first_name : 'Guest'
  \!
%h2 Welcome! It's SSO provider.
%p.flash
  = flash[:notice]
- if signed_in?
  %p= link_to("Logout", destroy_user_session_path)
- else
  %p= link_to("Login", new_user_session_path)
  %p= link_to("Sign Up", new_user_registration_path)
